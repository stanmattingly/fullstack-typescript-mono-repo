# Use a lightweight Node.js image
FROM node:23-bookworm-slim

# Set working directory inside the container
WORKDIR /app

# Copy the whole monorepo (including root and all packages)
COPY . .

# Install dependencies with npm, which supports workspaces
RUN npm install